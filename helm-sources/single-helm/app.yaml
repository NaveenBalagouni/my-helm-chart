apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-helm-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/NaveenBalagouni/my-helm-chart
    path: my-helm-charts/single-helm-sample
    targetRevision: main
    helm:
      valueFiles:
      - my-helm-charts/single-helm-sample/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: naveen
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
